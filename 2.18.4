speed(3)

length = 50

def square():
    penup()
    setposition(0,0)
    right(90)
    forward(length / 2)
    left(90)
    backward(length / 2)
    pendown()
    for i in range(4):
        forward(length)
        left(90)
        
while length <= 400:
    square()
    length = length + 50
