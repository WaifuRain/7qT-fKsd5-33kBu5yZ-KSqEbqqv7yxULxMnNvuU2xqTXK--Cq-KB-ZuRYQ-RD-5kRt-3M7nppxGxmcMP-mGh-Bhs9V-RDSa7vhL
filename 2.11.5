speed(5)
penup()
setposition(-150,0)

length = 10

def draw_square():
    for i in range(4):
        pendown()
        forward(length)
        left(90)
        penup()
    forward(length * 2)

for i in range(5):
    draw_square()
    length = length + 10
